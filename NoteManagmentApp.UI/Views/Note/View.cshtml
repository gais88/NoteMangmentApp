@model UpdateNoteVM
@{
    ViewData["Title"] = "View";
}

<h1>View</h1>

<form asp-controller="note" enctype="multipart/form-data">
    <div class="row">
        <div class="col-md-6">
            <div class="form-group">
                <label asp-for="Title" class="form-label mt-4"></label>
                <input type="text" class="form-control" asp-for="Title" placeholder="Title" readonly>
            </div>


            <div class="form-group">
                <label asp-for="Content" class="form-label mt-4"></label>
                <textarea asp-for="Content" class="form-control" placeholder="Content" readonly></textarea>
            </div>
        </div>
        <div class="col-md-6 d-flex justify-content-center pt-4 overflow-hidden">
            <img class="cover-preview" alt="Cover" src="@($"{FileSettings.ImagesPath}/{Model.CurrentImage}")" />
        </div>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="~/js/note-form.js" asp-append-version="true"></script>
}
